<% if (!user) { %>

	<p>Welcome! Please <a href="/login">log in</a>.</p>
<% } else { %>
	<? comment.description = user.displayName.split(" ").slice(0).join(""); -?>
	<p>Hello, <%= comment.description %>. View your <a href="/profile">profile</a>.</p>
<% } %>
