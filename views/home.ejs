<% if (!user) { %>

	<p>Welcome! Please <a href="/login">log in</a>.</p>
<% } else { %>
	<% var firstName= user.displayName; %>
	<% var firstName=firstName.split(" "); %>
	<p>Hello, <%= firstName[0] %>. View your <a href="/profile">profile</a>.</p>
<% } %>
